#include <stdio.h>
#include <stdlib.h>

int main()
{
    int num;
    printf("Give me a Number to return you its HEXA DECIMAL value!");
    scanf("%d", &num);
    printf("Hexa value is:%x", num);
    return 0;
}
