#include <stdio.h>
#include <stdlib.h>

int main()
{
    char c;
    printf("Give me a char to convert to ASCII value");
    scanf("%c", &c);
    printf("ASCII value for character %c=%d",c,c);
    return 0;
}
